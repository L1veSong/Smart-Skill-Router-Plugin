# CHANGELOG

## v0.1.0 (2026-06-05)

首次公开发布。

### 核心功能

- **A 层关键词匹配**：11 条种子规则，正则匹配，零延迟。覆盖设计、调试、代码、论文、ASCII、金融、旅行、代码审查、架构、发布、论文检索
- **B 层语义匹配**：main / openai / ollama 三后端架构。预过滤 223→20 候选，LLM 精度匹配
- **Dashboard**：Web 管理界面（localhost:8766），规则增删改查、命中统计
- **自学习**：B 层连续 3 次命中自动升级到 A 层
- **降级路径**：B 层不可用时 A 层独立工作，插件崩溃不阻塞对话

### 技术特性

- 延迟写盘：A 层每 5 次命中落盘，防多会话覆盖
- 冷却制去重：同 skill 60 秒内不重复推荐
- 任务切换检测：显式关键词 + 短消息判断
- B 层结果缓存：60 秒内同意图复用
- 冷启动重试：ollama 后端超时后自动 warmup + 重试
- 零配置：main 后端默认复用 Hermes 主模型
